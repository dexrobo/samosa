load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

sh_binary(
    name = "check",
    srcs = ["check.sh"],
    data = [
        "//tools/format",
        "//tools/lint:mypy",
    ],
)
